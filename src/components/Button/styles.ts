import styled from 'styled-components'

export const Button = styled.button`
  font-weight: bold;
  padding: 0.5rem 2rem;
  font-size: 0.75rem;
  border: 2px solid #f9e00f;
  border-style: outset;
  background: #f9e00f;
  border-radius: 0.25rem;
  cursor: pointer;
  color: black;
  display: block;
`
