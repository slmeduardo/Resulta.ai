import { Story, Meta } from '@storybook/react'
import StreamsCard from '.'

export default {
  title: 'StreamsCard',
  component: StreamsCard
} as Meta

export const Default: Story = () => <StreamsCard />
